{
  "name": "rct2-free-play",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@openrct2/types": "latest",
    "esbuild": "^0.25.0",
    "openrct2-mocks": "^0.1.6",
    "typescript": "^5.0.0",
    "vitest": "^4.1.4"
  }
}
